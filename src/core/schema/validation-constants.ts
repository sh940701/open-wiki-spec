/** Minimum character count for the Why section (overview 14.2) */
export const WHY_MIN_LENGTH = 50;

/** Maximum delta entries per change (matching OpenSpec MAX_DELTAS_PER_CHANGE) */
export const MAX_DELTAS_PER_CHANGE = 10;
